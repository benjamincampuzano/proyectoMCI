{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "prisma generate",
    "dev": "prisma generate && nodemon index.js",
    "migrate": "prisma generate && prisma migrate deploy --schema=prisma/schema.prisma",
    "start": "prisma generate && prisma migrate deploy --schema=prisma/schema.prisma && node index.js",
    "postinstall": "prisma generate"
  },
  "keywords": [
    "iglesia",
    "prisma",
    "express",
    "node",
    "javascript",
    "dotenv",
    "nodemon",
    "helmet",
    "compression",
    "cors",
    "bcryptjs",
    "jsonwebtoken",
    "axios"
  ],
  "author": "Benjamin Campuzano",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "axios": "^1.16.1",
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "@prisma/client-runtime-utils": "^7.8.0",
    "prisma": "^7.8.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "node-cron": "^4.2.1",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
