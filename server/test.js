const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const modules = await prisma.seminarModule.findMany({ select: { type: true }, distinct: ['type'] }); console.log(modules); } main().finally(() => prisma.\());
